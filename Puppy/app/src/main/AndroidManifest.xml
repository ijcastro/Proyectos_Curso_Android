<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ejemplo.puppy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".menus.AcercaDe"></activity>
        <activity android:name=".menus.Contacto" />
        <activity
            android:name=".MascotasFavoritos"
            android:label="Mis Favoritos"
            android:parentActivityName=".MascotasListado" />
        <activity
            android:name=".MascotasListado"
            android:label="Mi Listado De Mascotas"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>